global:
  evaluation_interval: 30s
  scrape_interval: 5s
scrape_configs:
  - job_name: customer-api
    static_configs:
      - targets:
          - 'localhost:5264' # This doesn't work when Prometheus is inside a container :(